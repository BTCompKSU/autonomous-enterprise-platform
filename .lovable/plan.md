
Add `"Invoicing"` to the `skills` array of the **Finance and Accounting** category in `src/lib/job-categories.ts`.

One-line change — appended to the existing 30-skill list. The new chip will show up in Step 2 for anyone who picks Finance and Accounting, selectable/toggleable like every other preset skill. No other files need changes.

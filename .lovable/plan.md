
Quick header tweak in `src/components/AppShell.tsx`:

1. Remove the standalone "Home" nav item (the logo already links to `/`).
2. Reorder + rename remaining nav links:
   - `/opportunity` → "Opportunity Landscape"
   - `/dashboard` → "Executive Audit"
   - `/employee` → "Employee Analysis"
   - `/skill-module` → "Agent Builder"
3. Leave the logo (icon + "UpSkill USA") as the home link — no other changes.

No new routes, no other files touched.

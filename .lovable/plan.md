

## Plan: Collapse onboarding from 4 steps to 3 (remove old Step 1)

The old Step 1 ("What do you do all day?" — role picker) goes away. The current Step 2 (department picker) becomes the new Step 1. Steps 3 and 4 shift up to become Steps 2 and 3.

### Renames (logical reordering)

| Old | New | Purpose |
|---|---|---|
| step-1 (role) | **removed** | — |
| step-2 (department) | **step-1** | Department |
| step-3 (tasks) | **step-2** | Tasks |
| step-4 (analyze) | **step-3** | Analyze |

### File changes

1. **Delete** `src/routes/onboarding.step-1.tsx` (old role picker) and its top-level redirect `src/routes/step-1.tsx`.

2. **Rename + rewrite route paths**:
   - `onboarding.step-2.tsx` → `onboarding.step-1.tsx`, change `createFileRoute("/onboarding/step-2")` → `"/onboarding/step-1"`. Remove the "← role chip" block and the "← Back" link (it's now the first step). "Continue" goes to `/onboarding/step-2`.
   - `onboarding.step-3.tsx` → `onboarding.step-2.tsx`, update route path and Back link to `/onboarding/step-1`, Continue to `/onboarding/step-3`.
   - `onboarding.step-4.tsx` → `onboarding.step-3.tsx`, update route path. Final auto-redirect to `/workflowai` stays.

3. **Update top-level redirects** in `src/routes/step-2.tsx`, `step-3.tsx`, `step-4.tsx` so they redirect to the new paths (or just delete `step-4.tsx` since there's no step 4 anymore). Keep `step-1.tsx` redirecting to `/onboarding/step-1`.

4. **`src/routes/onboarding.tsx`** — Update `STEPS` array to 3 entries: Department, Tasks, Analyze. Progress bar auto-adjusts. Logo link target updates from `/onboarding/step-1` (still valid, now points to department).

5. **`src/routes/onboarding.index.tsx`** — Already redirects to `/onboarding/step-1`, no change needed (target is now department picker).

6. **`src/lib/onboarding-store.ts`** — Remove `selected_role` field from `OnboardingProfile` interface and `EMPTY` default, since the role-picker step is gone. Search for any other consumers (`workflowai`, dashboards) and clean references.

7. **`src/lib/job-categories.ts`** — Keep `ENTERPRISE_JOB_CATEGORIES` (used by new Step 1). Remove `QUICK_ROLES` and `fuzzyMatchRoles` exports if not used elsewhere — I'll grep first.

8. **`src/routeTree.gen.ts`** — Auto-regenerated by the Vite plugin; no manual edit.

### Validation before edits

I'll grep for `selected_role`, `QUICK_ROLES`, `fuzzyMatchRoles`, and `onboarding/step-` references across the codebase to make sure nothing else breaks.

